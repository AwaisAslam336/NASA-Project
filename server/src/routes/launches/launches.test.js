describe('Test GET /launches',()=>{
    test('it should respond with 200 request',()=>{
        const response = 200;
        expect(response).toBe(200);
    })
})

describe('Test POST /launches',()=>{
    test('it should respond with 200 request', ()=>{
        const response = 200;
        expect(response).toBe(200);
    });
    test('it should catch missing required properties',()=>{

    });
    test('it should catch invalid dates',()=>{
        
    });
})